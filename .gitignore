.venv/
__pycache__/
*.pyc
.DS_Store
.Rhistory

# Regenerated in ~15s by: python -m sgw_platform.datagen.generate --out data
# Small CSV extracts ARE committed so the prototype can be assessed without
# running anything; the large derived tables are not.
data/hazard_exposure.parquet
data/scada_telemetry.parquet
data/training_table.parquet

# Model outputs, regenerated by scripts/run_w*.py
artifacts/*.parquet

# LaTeX build artefacts
deliverables/tex/*.aux
deliverables/tex/*.log
deliverables/tex/*.out
deliverables/tex/*.png

# Working scratch
case-pages/
