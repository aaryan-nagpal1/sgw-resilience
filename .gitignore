.venv/
__pycache__/
*.pyc
.DS_Store
.Rhistory
node_modules/

# Regenerated in ~15s by: python -m sgw_platform.datagen.generate --out data
# The small CSV extracts ARE committed so the prototype can be assessed without
# running anything; the large derived tables are not.
data/hazard_exposure.parquet
data/scada_telemetry.parquet
data/training_table.parquet

# Model outputs; the CSV metrics are committed, the large frames are not
artifacts/*.parquet

# Written deliverables and their sources are submitted by email, not committed
deliverables/
case-pages/

# Personal working notes, not part of the submission
0[0-9]-*.md
10-*.md

# Local tooling config, not part of the project
.claude/
.agents/

# The assignment brief itself
AECOM-AI-Solution Eng Case.pdf

# Diagrams belonging to the written deliverables, not produced by the scripts
figures/architecture.png
figures/exec_flow.png
figures/exec_value.png
